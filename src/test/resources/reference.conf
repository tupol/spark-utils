# The only property of the SchemaFactory application that needs to have a value is the inferSchema.
# Since inferring the schema is essentially the job of SchemaFactory this should always be true.
schemafactory: {
  inferSchema = true
}

# Configuration section for the SparkRunnableSpec test
MockApp: {
  whoami: "./src/test/resources/reference.conf"
  reference: "reference_mock_app"
  classpath.application.conf: false
}

# Configuration section for the SparkRunnableSpec test
MockFun: {
  whoami: "./src/test/resources/reference.conf"
  reference: "reference_mock_fun"
  classpath.application.conf: false
}
